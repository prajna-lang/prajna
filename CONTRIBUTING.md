# 开发者须知

* 遵行现有的代码风格
* 必须使用clang-format插件来整理代码
* 建议使用editorconfig插件
* 推荐使用vscode, 若使用服务器, 推荐使用sshremote插件
* 我们的代码签入使用 linear history的commit方式, 意味着所有的PR都是git merge --squash的形式合入的
* 提交PR之前需要rebase的最新的开发者分支, 目前的开发者分支为preview分支
