#stage0; // 加入bool, char, void, undef类型
#stage1; // 加入int, float, __add等模板和操作

use int<8> as i8;
use int<16> as i16;
use int<32> as i32;
use int<64> as i64;

use uint<8> as u8;
use uint<16> as u16;
use uint<32> as u32;
use uint<64> as u64;

use float<16> as f16;
use float<32> as f32;
use float<64> as f64;

use operator;

use reference_count;
use reference_count::ReferenceCountable;

use bindings;
use debug;

use array;
use array::Array;

use __ptr;
use __ptr::ptr;

use __type;

use dynamic_array;
use dynamic_array::DynamicArray;

use __str;
use __str::str;
use __str::print;
use __str::printLine;


use serializable;
use serializable::Serializable;

use serialize;

use tensor;
use tensor::Tensor;
use tensor::Layout;

use list;

// use io;
