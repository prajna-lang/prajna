@test
func TestMain(){
    var a = true;
    var b = false;

    test::Assert(a & a);
    test::Assert(a | b);
    test::Assert(a ^ b);

    test::Assert(!b);
    // test::Assert(!a.__equal__(false));
}
