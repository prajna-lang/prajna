/// 通过该文件导出符号, 这样后面才能索引到
func print_c(str : char*);

func assert(flag : bool);

func malloc(bytes: i64)->undef*;
func free(pointer: undef*);

func registerReferenceCount(pointer: undef*);
func getReferenceCount(pointer: undef*)->i64;
func incReferenceCount(pointer: undef*);
func decReferenceCount(pointer: undef*);

export print_c;
export assert;
export malloc;
export free;
