#stage0; // 加入bool, char, void, undef类型
#stage1; // 加入int, Float, __add等模板和操作

use Int<8> as i8;
use Int<16> as i16;
use Int<32> as i32;
use Int<64> as i64;
use Int<128> as i128;

use Uint<8> as u8;
use Uint<16> as u16;
use Uint<32> as u32;
use Uint<64> as u64;
use Uint<128> as u128;

use Float<16> as f16;
use Float<32> as f32;
use Float<64> as f64;
// use Float<128> as f128; // 平台支持不好, 除+,-,*,/外, sin, cos等函数都是错误的结果.

use operator;

use disable_dynamic_interface::ReferenceCountable;
use disable_dynamic_interface::Initializable;
use disable_dynamic_interface::Finalizable;

use bindings;
use debug;
use test;

use array::Array;

use __type;
use __float::FloatFixedPrinter;
use __float::FloatScientificPrinter;
use __int;

use _ptr::Ptr;
use _dynamic;

use atomic;

use dynamic_array::DynamicArray;
use tensor::Tensor;
use tensor::Layout;
use list::Node;
use list::List;
use dict::ListDict;

use __str::String;
use __str::Print;
use __str::PrintLine;

use serializable::Serializable;
use serialize;

use io::Input;
