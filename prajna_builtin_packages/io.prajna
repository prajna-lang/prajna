func read()->str{
    var str_line = str::create(1);
    str_line[0] = bindings::getchar();
    while (str_line[str_line.size - 1].__not_equal__('\n')) {
        str_line.append(bindings::getchar());
    }

    return str_line;
}
